# Loaded by `vite build --mode demo` (npm run build:demo) and by the GitHub
# Pages workflow. Switches the interface to reading ui/public/demo/ instead of
# talking to a server — see DEMO in src/api.ts.
VITE_DEMO=1
